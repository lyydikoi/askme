.question
  .question-content
    .question-text.text-lead
      = question.text

    - if question.answer.present?
      .question-answer.text-lead
        = question.answer
    .question-date
      = question.created_at.strftime('%d %b %M:%H')
